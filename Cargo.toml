[package]
name = "spec-cli"
version = "0.1.0"
edition = "2024"
description = "CLI interface for spec-api: the `spec` command"

[[bin]]
name = "spec"
path = "src/bin/spec.rs"

[lib]
name = "spec_cli"
path = "src/lib.rs"

[dependencies]
spec-api = { path = "../../../crates/spec-api" }
ticket-api = { path = "../../../crates/ticket-api" }
memory-kernel = { git = "https://github.com/mankinskin/memory-kernel", branch = "main"  }
rule-api = { path = "../../../crates/rule-api" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toon-format = "0.5"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["serde", "v4"] }
thiserror = "2"
syn = { version = "2.0", features = ["full", "parsing", "visit"] }
proc-macro2 = { version = "1.0", features = ["span-locations"] }
walkdir = "2.4"

[dev-dependencies]
pretty_assertions = "1"
tempfile = "3"
